---
import * as Lucide from "lucide-astro";
import Container from "../layout/Container.astro";
import { featureIconMap } from "../../utils/featureIcon";
import type { KeyFeaturesSection } from "../../lib/types";
import Plant from "../../assets/images/svg/plant.svg"
import Reed1 from "../../assets/images/svg/reed1.svg"

interface Props extends KeyFeaturesSection {}

const { items } = Astro.props as Props;
---

<section class="bg-surface-alt shadow-2 overflow-hidden relative">
  <Reed1 class="decorative -bottom-10 left-[4%] sm:left-[5%] w-22 text-dark/10 z-0" />
  <Plant class="decorative -bottom-10 right-[2%] sm:right-[5%] w-34 text-dark/10 z-0" />
  <Container>
    <div class="grid grid-cols-2 lg:grid-cols-4">
      {items.map((item, index) => {
        const Icon = featureIconMap[item.icon] ?? Lucide.Circle;

        return (
          <div
            class:list={[
              "card-hover flex flex-col items-center text-center card-padding border-b border-border",
              index % 2 === 1 && "border-l border-border",
              "lg:border-l",
              index % 4 === 0 && "lg:border-l-0",
            ]}
          >
            <div class="cluster-sm mb-sm h-2xl w-2xl">
              <Icon
                class="h-lg w-lg text-primary"
                aria-hidden="true"
              />
            </div>

            <h3 class="subheading">
              {item.title}
            </h3>

            {item.description && (
              <p class="body text-soft container-sm mt-sm hidden sm:block max-w-[220px]">
                {item.description}
              </p>
            )}
          </div>
        );
      })}
    </div>
  </Container>
</section>